name: Build Dual Relay APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'android-dual-apk/**'
      - '.github/workflows/build-dual-relay-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-dual-apk
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        run: gradle assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: tessa-gptina-relay-apk
          path: android-dual-apk/app/build/outputs/apk/debug/app-debug.apk
